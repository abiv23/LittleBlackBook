<div class="container">
    <div class="row profile-header">
        <h3>How'd the date go?</h3>
    </div>
    <div class="col s12 m9">
    <h5>Your date with {{date.name}} on {{date.date}} at {{date.location}}</h5>
    <img src="{{date.image_url}}" class="responsive-image">
    <form method="post" action="/review_date/?_method=put">
        <div class="input-field col s6 m6">
          Rating:
          <input name="rating" type="number" min=1 max=10>
        </div>
        <div class="input-field col s6 m6">
          <textarea name="note" placeholder="Enter Date Review..."></textarea>
        </div><br>
        <button class="btn red" type="reset" name="cancel">Clear Changes</button>
        <button class="btn green" type="submit">Save</button>
    </form>
  </div><br>

</div>
